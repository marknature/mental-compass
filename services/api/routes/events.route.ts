// routes/events.ts
import express from "express";
const router = express.Router();

// Events and challenges routes
router.post("/", (req, res) => {});
router.get("/", (req, res) => {});

export default router;
